;; -*- mode: emacs-lisp -*-
;; vim: filetype=lisp
;; This file is loaded by Spacemacs at startup.
;; It must be stored in your home directory.
;; (kbd "SPC f e R") to reload

;; Custom stuff is per-install, and breaks other instances.
;; Move all configuration somewhere that Custom doesn't touch.
(load "~/.spacemacs-user")
